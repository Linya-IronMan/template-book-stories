# AGENTS 规范

## 通用硬性要求

1. 本仓库所有文档、测试说明、注释必须使用简体中文。
2. 代码内必须提供方法级中文注释（JSDoc 风格）。
3. 禁止使用 JS/TS class 私有字段 `#xxx` 语法，保持现有代码风格。
4. 本项目以 `docs/*.md` 作为唯一规范来源，所有实现与测试必须严格遵守该文档。
5. 凡是需要提交 Commit，必须先阅读 `docs/maintenance/git-commit.md`，并严格按其中规范编写提交信息。
6. 如果需要修改代码，先生成方案，经过批准之后再按照方案实现代码

## 文档工作流

1. 开始动手前，先阅读 `docs/index.md`。
2. 根据任务类型，从 `docs/index.md` 找到对应规范文档，再继续实现、测试或改文档。
3. 若改动影响多个模块，必须同时阅读对应的多份规范文档，不能只看单一模块文档。
4. 若实现、测试或目录结构发生变化，必须同步更新相关规范文档与 `docs/index.md`。

## 文件规模与职责边界

- 单文件超过 3000 行时，不得继续无边界累积，必须评估是否拆分，并说明结论。
- 若单文件超过 3000 行，且同时承载多个职责（如 view / state orchestration / service / adapter / side effects 混杂），则应优先拆分。
- 拆分目标不是机械缩短行数，而是形成稳定职责边界，使后续需求尽量能在单一文件或单一层内完成修改。
- 拆分时应优先按职责边界组织，例如：视图组件、状态编排、领域服务、协议适配、副作用管理；不得按无明确语义的方式随意切文件。
- 以下情况可以暂不拆分，但必须在提交说明中写清理由：
    - 强耦合逻辑必须整体阅读
    - 生成代码或协议产物，不适合人工拆分
    - 历史兼容层，当前阶段以稳定为先
- 禁止为了拆分而拆分，禁止产出无语义的 `utils.ts`、`helpers.ts`、`common.ts` 式文件。
